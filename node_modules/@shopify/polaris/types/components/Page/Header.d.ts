/// <reference types="react" />
import * as React from 'react';
import { IconableAction, DisableableAction } from '../../types';
import { Props as BreadcrumbProps } from '../Breadcrumbs';
import { PaginationDescriptor } from '../Pagination';
export interface ActionGroup {
    title: string;
    icon?: IconableAction['icon'];
    actions: IconableAction[];
}
export interface Props {
    title: string;
    icon?: string;
    separator?: boolean;
    breadcrumbs?: BreadcrumbProps['breadcrumbs'];
    secondaryActions?: IconableAction[];
    primaryAction?: DisableableAction;
    pagination?: PaginationDescriptor;
    actionGroups?: ActionGroup[];
}
export interface State {
    openActionGroup?: string;
    rollupOpen: boolean;
}
export default class Header extends React.PureComponent<Props, State> {
    state: State;
    private readonly hasRollup;
    render(): JSX.Element;
    private renderSecondaryActions();
    private handleRollupToggle();
    private handleActionGroupClose(group);
    private handleActionGroupOpen(group);
}
