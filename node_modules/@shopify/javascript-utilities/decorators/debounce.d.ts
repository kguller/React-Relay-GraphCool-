/// <reference types="lodash" />
import { DebounceSettings } from 'lodash';
export default function debounce(wait?: number, options?: DebounceSettings): (target: any, key: string, descriptor: PropertyDescriptor) => {
    configurable: boolean;
    get(): any;
};
